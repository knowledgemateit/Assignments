---
*Author:* knowledgemateit  
**LinkedIn:** https://www.linkedin.com/in/knowledgemate/  
**Email:** knowledgemateit@gmail.com  
**Created:** July 3, 2026
---


### **Assignment: AWS SNS & CloudFormation - Notifications and Infrastructure as Code**

This assignment covers **SNS** for notifications and **CloudFormation** for IaC.

---

## **Objectives**

- ✅ Understand SNS messaging
- ✅ Create topics and subscriptions
- ✅ Send notifications
- ✅ Understand CloudFormation
- ✅ Create templates
- ✅ Deploy stacks

---

## **Part 1: SNS (Simple Notification Service)**

### **Create Topic**

```bash
# Create SNS topic
aws sns create-topic --name my-alerts

# Get topic ARN
TOPIC_ARN=$(aws sns list-topics --query 'Topics[0].TopicArn' --output text)

# List topics
aws sns list-topics
```

### **Subscribe to Topic**

```bash
# Email subscription
aws sns subscribe \
  --topic-arn $TOPIC_ARN \
  --protocol email \
  --notification-endpoint user@example.com

# SMS subscription
aws sns subscribe \
  --topic-arn $TOPIC_ARN \
  --protocol sms \
  --notification-endpoint +1234567890

# Lambda subscription
aws sns subscribe \
  --topic-arn $TOPIC_ARN \
  --protocol lambda \
  --notification-endpoint arn:aws:lambda:us-east-1:123456789012:function:MyFunction

# HTTP subscription
aws sns subscribe \
  --topic-arn $TOPIC_ARN \
  --protocol http \
  --notification-endpoint http://example.com/webhook
```

### **Publish Messages**

```bash
# Publish message
aws sns publish \
  --topic-arn $TOPIC_ARN \
  --message "Alert: Server CPU high"

# Publish with subject
aws sns publish \
  --topic-arn $TOPIC_ARN \
  --subject "System Alert" \
  --message "Critical alert: CPU > 90%"

# Publish with message attributes
aws sns publish \
  --topic-arn $TOPIC_ARN \
  --message "Alert" \
  --message-attributes severity=String,string_value=HIGH
```

---

## **Part 2: CloudFormation Fundamentals**

CloudFormation provides:
- Infrastructure as Code (IaC)
- Template-based deployment
- Stack management
- Change sets
- Automatic rollback

### **Template Structure**

```yaml
AWSTemplateFormatVersion: '2010-09-09'
Description: 'Simple EC2 instance template'

Parameters:
  InstanceType:
    Type: String
    Default: t3.micro
    Description: EC2 instance type

Resources:
  MyInstance:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: ami-0c55b159cbfafe1f0
      InstanceType: !Ref InstanceType
      Tags:
        - Key: Name
          Value: MyInstance

Outputs:
  InstanceId:
    Value: !Ref MyInstance
    Description: Instance ID
```

---

## **Part 3: Create and Deploy Stack**

```bash
# Create stack from template
aws cloudformation create-stack \
  --stack-name my-stack \
  --template-body file://template.yaml \
  --parameters ParameterKey=InstanceType,ParameterValue=t3.micro

# Monitor stack creation
aws cloudformation describe-stacks --stack-name my-stack

# Wait for stack to complete
aws cloudformation wait stack-create-complete --stack-name my-stack

# View stack resources
aws cloudformation describe-stack-resources --stack-name my-stack

# Get stack outputs
aws cloudformation describe-stacks \
  --stack-name my-stack \
  --query 'Stacks[0].Outputs'
```

---

## **Part 4: CloudFormation Template Examples**

### **VPC and Networking**

```yaml
Resources:
  MyVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.0.0.0/16
      Tags:
        - Key: Name
          Value: MyVPC

  PublicSubnet:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref MyVPC
      CidrBlock: 10.0.1.0/24
      AvailabilityZone: us-east-1a

  InternetGateway:
    Type: AWS::EC2::InternetGateway

  AttachGateway:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      VpcId: !Ref MyVPC
      InternetGatewayId: !Ref InternetGateway
```

### **Load Balancer and Auto Scaling**

```yaml
Resources:
  MyLoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Subnets:
        - !Ref PublicSubnet
      SecurityGroups:
        - !Ref LBSecurityGroup

  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Port: 80
      Protocol: HTTP
      VpcId: !Ref MyVPC

  MyAutoScalingGroup:
    Type: AWS::AutoScaling::AutoScalingGroup
    Properties:
      LaunchTemplate:
        LaunchTemplateId: !Ref LaunchTemplate
        Version: !GetAtt LaunchTemplate.LatestVersionNumber
      MinSize: 2
      MaxSize: 6
      DesiredCapacity: 3
      TargetGroupARNs:
        - !Ref TargetGroup
```

---

## **Part 5: Stack Updates and Deletion**

```bash
# Update stack
aws cloudformation update-stack \
  --stack-name my-stack \
  --template-body file://updated-template.yaml

# Create change set
aws cloudformation create-change-set \
  --stack-name my-stack \
  --change-set-name my-changes \
  --template-body file://template.yaml

# Execute change set
aws cloudformation execute-change-set \
  --stack-name my-stack \
  --change-set-name my-changes

# Delete stack
aws cloudformation delete-stack --stack-name my-stack

# Wait for deletion
aws cloudformation wait stack-delete-complete --stack-name my-stack
```

---

## **Practice Exercises**

```bash
# Exercise 1: SNS Notifications
# 1. Create SNS topic
# 2. Subscribe with email
# 3. Publish message
# 4. Verify notification

# Exercise 2: CloudFormation Basic
# 1. Create template with EC2 instance
# 2. Deploy stack
# 3. Verify resources created
# 4. Update stack (change instance type)
# 5. Delete stack

# Exercise 3: Complete Stack
# 1. Create template with:
#    - VPC and subnets
#    - Security groups
#    - EC2 instances
#    - Load balancer
# 2. Deploy stack
# 3. Test connectivity
# 4. Clean up
```

---

## **Summary**

✅ Created SNS topics and subscriptions  
✅ Published notifications  
✅ Created CloudFormation templates  
✅ Deployed stacks  
✅ Updated and deleted stacks  

**Next Steps:** Master AWS architectures and best practices! 🚀
