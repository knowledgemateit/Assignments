---
*Author:* knowledgemateit  
**LinkedIn:** https://www.linkedin.com/in/knowledgemate/  
**Email:** knowledgemateit@gmail.com  
**Created:** July 3, 2026
---


### **Assignment: AWS Auto Scaling - Automatic Instance Management**

This assignment teaches **Auto Scaling** for automatic capacity management.

---

## **Objectives**

- ✅ Understand Auto Scaling concepts
- ✅ Create launch templates
- ✅ Create Auto Scaling groups
- ✅ Configure scaling policies
- ✅ Monitor scaling events
- ✅ Implement load balancer integration

---

## **Part 1: Auto Scaling Fundamentals**

Auto Scaling automatically:
- Scales up during high demand
- Scales down during low demand
- Maintains desired capacity
- Works with load balancers

---

## **Part 2: Create Launch Template**

```bash
# Create launch template
aws ec2 create-launch-template \
  --launch-template-name my-template \
  --launch-template-data '{
    "ImageId": "ami-0c55b159cbfafe1f0",
    "InstanceType": "t3.micro",
    "KeyName": "my-key-pair",
    "SecurityGroupIds": ["sg-xxxxx"],
    "UserData": "IyEvYmluL2Jhc2gKYXB0IHVwZGF0ZQphcHQgaW5zdGFsbCAteSBhcGFjaGUyCnN5c3RlbWN0bCBzdGFydCBhcGFjaGUy"
  }'

# List templates
aws ec2 describe-launch-templates
```

---

## **Part 3: Create Auto Scaling Group**

```bash
# Create Auto Scaling group
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name my-asg \
  --launch-template LaunchTemplateName=my-template,Version='$Latest' \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 3 \
  --default-cooldown 300 \
  --vpc-zone-identifier "subnet-xxxxx,subnet-yyyyy" \
  --target-group-arns $TG_ARN \
  --health-check-type ELB \
  --health-check-grace-period 300

# Describe ASG
aws autoscaling describe-auto-scaling-groups \
  --auto-scaling-group-names my-asg
```

---

## **Part 4: Scaling Policies**

```bash
# Target tracking scaling (recommended)
aws autoscaling put-scaling-policy \
  --auto-scaling-group-name my-asg \
  --policy-name cpu-scaling \
  --policy-type TargetTrackingScaling \
  --target-tracking-configuration '{
    "TargetValue": 70.0,
    "PredefinedMetricSpecification": {
      "PredefinedMetricType": "ASGAverageCPUUtilization"
    }
  }'

# Step scaling (custom)
aws autoscaling put-scaling-policy \
  --auto-scaling-group-name my-asg \
  --policy-name scale-up \
  --policy-type StepScaling \
  --adjustment-type ChangeInCapacity \
  --metric-aggregation-type Average \
  --step-adjustments MetricIntervalLowerBound=0,MetricIntervalUpperBound=10,ScalingAdjustment=1
```

---

## **Part 5: Monitor Scaling**

```bash
# View scaling activities
aws autoscaling describe-scaling-activities \
  --auto-scaling-group-name my-asg

# View running instances
aws autoscaling describe-auto-scaling-groups \
  --auto-scaling-group-names my-asg \
  --query 'AutoScalingGroups[0].Instances'

# View CloudWatch metrics
aws cloudwatch get-metric-statistics \
  --namespace AWS/AutoScaling \
  --metric-name GroupDesiredCapacity \
  --dimensions Name=AutoScalingGroupName,Value=my-asg \
  --start-time 2024-01-01T00:00:00Z \
  --end-time 2024-01-02T00:00:00Z \
  --period 300 \
  --statistics Average
```

---

## **Part 6: Advanced Configuration**

```bash
# Lifecycle hooks
aws autoscaling put-lifecycle-hook \
  --lifecycle-hook-name my-hook \
  --auto-scaling-group-name my-asg \
  --lifecycle-transition autoscaling:EC2_INSTANCE_LAUNCHING \
  --default-result CONTINUE \
  --heartbeat-timeout 300

# Scheduled actions
aws autoscaling put-scheduled-update-group-action \
  --auto-scaling-group-name my-asg \
  --scheduled-action-name scale-up-morning \
  --recurrence "0 9 * * MON-FRI" \
  --desired-capacity 5
```

---

## **Practice Exercises**

```bash
# Exercise 1: Basic Auto Scaling
# 1. Create launch template
# 2. Create Auto Scaling group (min:2, max:6, desired:3)
# 3. Wait for instances to launch
# 4. Verify instances running

# Exercise 2: Scaling policies
# 1. Create target tracking policy (CPU 70%)
# 2. Create CloudWatch alarms
# 3. Stress test instances
# 4. Monitor scaling up
# 5. Stop stress test
# 6. Monitor scaling down

# Exercise 3: Load balancer integration
# 1. Create ALB and target group
# 2. Create ASG attached to target group
# 3. Configure health checks
# 4. Launch and test
```

---

## **Summary**

✅ Created launch templates  
✅ Created Auto Scaling groups  
✅ Configured scaling policies  
✅ Monitored scaling events  
✅ Integrated with load balancers  

**Next Steps:** Learn CloudFormation and infrastructure as code! 🚀
